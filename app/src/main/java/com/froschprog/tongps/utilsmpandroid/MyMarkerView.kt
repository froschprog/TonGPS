package com.froschprog.tongps.utilsmpandroid

/*

open class MyMarkerView(context: Context?, layoutResource: Int) : MarkerView(context, layoutResource) {

    private var tvContent: TextView? = null

    open fun MyMarkerView(context: Context?, layoutResource: Int) {
        //super(context, layoutResource)
        // this markerview only displays a textview
        tvContent = findViewById<View>(R.id.tvContent) as TextView
    }

// callbacks everytime the MarkerView is redrawn, can be used to update the
// content (user-interface)

    // callbacks everytime the MarkerView is redrawn, can be used to update the
    // content (user-interface)
    override fun refreshContent(e: Map.Entry<*, *>, highlight: Highlight?) {
        // here you can change whatever you want to show in following line as x/y or both
        tvContent!!.text = "x: " + e.getX().toString() + " , y: " + e.getY() // set the entry-value as the display text
    }


}*/
